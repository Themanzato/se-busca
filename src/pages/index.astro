---
// Posada Navideña - Invitación Oficial
const fechaEvento = new Date('2024-12-20T21:00:00');
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Estás cordialmente invitado a la Gran Posada Navideña 2025 en Salon luna, Actopan, Veracruz" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:title" content="Gran Posada Navideña 2025 - Actopan, Veracruz" />
    <meta property="og:description" content="Estás cordialmente invitado a la Gran Posada Navideña. 20 de Diciembre, 10:00 PM - Salon luna" />
    <meta property="og:type" content="website" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700;900&family=Cormorant+Garamond:wght@300;400;500;600&family=Great+Vibes&display=swap" rel="stylesheet">
    <title>Gran Posada Navideña 2025 | Actopan, Veracruz</title>
  </head>
  <body>
    <!-- Copos de nieve animados -->
    <div class="snowflakes" aria-hidden="true">
      {Array.from({length: 20}).map((_, i) => (
        <div class="snowflake" style={`--delay: ${Math.random() * 10}s; --left: ${Math.random() * 100}%; --size: ${0.5 + Math.random() * 1}rem; --duration: ${8 + Math.random() * 10}s;`}>❄</div>
      ))}
    </div>

    <!-- Decoración superior -->
    <div class="top-garland">
      <div class="garland-lights">
        {Array.from({length: 30}).map((_, i) => (
          <span class="light" style={`--delay: ${i * 0.1}s`}></span>
        ))}
      </div>
    </div>

    <main>
      <!-- Header con invitación -->
      <header class="hero">
        <div class="ornament left">🎄</div>
        <div class="ornament right">🎄</div>
        
        <p class="subtitle-top">Estás Cordialmente Invitado a la</p>
        <h1 class="title">
          <span class="title-line">Gran <span class="posada-wrapper"><span class="peda-tachada">Peda</span>Posada</span></span>
          <span class="title-line accent">Navideña</span>
          <span class="year">2025</span>
        </h1>
        
        <div class="divider">
          <span>✦</span>
          <span class="line"></span>
          <span>🎅</span>
          <span class="line"></span>
          <span>✦</span>
        </div>

        <p class="invitation-text">
          Eres libre de llevar a cualquier persona que tenga ganas de tomar, 
          <br><strong>los azulitos van de nuestra parte, pero el ambiente lo pones tú.</strong>
        </p>
      </header>

      <!-- Información del evento -->
      <section class="event-info">
        <div class="info-card date-card">
          <div class="icon">📅</div>
          <h3>Fecha</h3>
          <p class="main-info">20 de Diciembre</p>
          <p class="sub-info">Sabado, 2025</p>
        </div>

        <div class="info-card time-card">
          <div class="icon">🕘</div>
          <h3>Hora</h3>
          <p class="main-info">10:00 PM</p>
          <p class="sub-info">(Llega a la hora que quieras)</p>
        </div>

        <div class="info-card location-card">
          <div class="icon">📍</div>
          <h3>Lugar</h3>
          <p class="main-info">Salon luna</p>
          <p class="sub-info">Actopan, Veracruz</p>
        </div>
      </section>

      


      <!-- Ubicación -->
      <section class="location-section">
        <h2>Ubicación</h2>
        <div class="location-content">
          <div class="address-box">
            <div class="pin-icon">📍</div>
            <div class="address-text">
              <h3>Salon luna</h3>
              <p>Actopan, Veracruz, México</p>
            </div>
          </div>
          
          <!-- Mapa embebido -->
          <div class="map-container">
            <iframe 
              src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d939.8089!2d-96.6288208!3d19.4690783!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zMTnCsDI4JzA4LjciTiA5NsKwMzcnNDMuOCJX!5e0!3m2!1ses!2smx!4v1234567890"
              width="100%" 
              height="300" 
              style="border:0; border-radius: 15px;" 
              allowfullscreen="" 
              loading="lazy" 
              referrerpolicy="no-referrer-when-downgrade">
            </iframe>
          </div>

          <a href="https://maps.app.goo.gl/6NZ6BSvoVkRLbXMAA" target="_blank" rel="noopener noreferrer" class="map-button">
            <span>🗺️</span> Abrir en Google Maps
          </a>
        </div>
      </section>

      <!-- Contacto WhatsApp -->
      <section class="contact-section">
        <h2>¿Tienes Dudas?</h2>
        <p>Únete al grupo de WhatsApp para confirmar asistencia, preguntas y más información</p>
        <a href="https://chat.whatsapp.com/FdeIB5ZFZGh8XpY2UqgYQi" target="_blank" rel="noopener noreferrer" class="whatsapp-button">
          <svg class="whatsapp-icon" viewBox="0 0 24 24" fill="currentColor">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
          </svg>
          Unirse al Grupo de WhatsApp
        </a>
      </section>

    

      <!-- Footer -->
      <footer>
        <div class="footer-decoration">
          <span>🎄</span>
          <span>⭐</span>
          <span>🎄</span>
        </div>
        <p class="footer-text">Te esperamos para la ultima peda masiva del año</p>
        <p class="footer-small">Posada Navideña 2025 • Actopan, Veracruz</p>
        
        <!-- Créditos del desarrollador -->
        <div class="dev-credits">
          <p>Web creada por <strong>Mario Alberto Utrera</strong> <span class="no-pay">(No me pagaron pipipi)</span></p>
          <p class="contact-dev">
            Contacto para proyectos similares: 
            <a href="https://www.facebook.com/themanzato" target="_blank" rel="noopener noreferrer" class="facebook-link">
              <svg class="facebook-icon" viewBox="0 0 24 24" fill="currentColor">
                <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
              </svg>
            </a>
          </p>
        </div>
      </footer>
    </main>

    <script>
      // Countdown Timer
      function updateCountdown() {
        const eventDate = new Date('2024-12-20T21:00:00').getTime();
        const now = new Date().getTime();
        const distance = eventDate - now;

        const countdownEl = document.getElementById('countdown');
        if (distance < 0) {
          if (countdownEl) countdownEl.innerHTML = '<p class="event-started">¡La posada ya comenzó! 🎉</p>';
          return;
        }

        const days = Math.floor(distance / (1000 * 60 * 60 * 24));
        const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((distance % (1000 * 60)) / 1000);

        const daysEl = document.getElementById('days');
        const hoursEl = document.getElementById('hours');
        const minutesEl = document.getElementById('minutes');
        const secondsEl = document.getElementById('seconds');

        if (daysEl) daysEl.textContent = String(days).padStart(2, '0');
        if (hoursEl) hoursEl.textContent = String(hours).padStart(2, '0');
        if (minutesEl) minutesEl.textContent = String(minutes).padStart(2, '0');
        if (secondsEl) secondsEl.textContent = String(seconds).padStart(2, '0');
      }

      updateCountdown();
      setInterval(updateCountdown, 1000);

      // Animación de entrada al scroll
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('visible');
          }
        });
      }, { threshold: 0.1 });

      document.querySelectorAll('section, .info-card, .activity').forEach(el => {
        observer.observe(el);
      });
    </script>
  </body>
</html>

<style>
  :root {
    --christmas-red: #C41E3A;
    --christmas-red-dark: #8B0000;
    --christmas-green: #165B33;
    --christmas-green-light: #146B3A;
    --gold: #FFD700;
    --gold-dark: #B8860B;
    --cream: #FFF8E7;
    --dark-bg: #0D1117;
    --card-bg: rgba(255, 255, 255, 0.05);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Cormorant Garamond', Georgia, serif;
    background: linear-gradient(180deg, 
      #0D1117 0%, 
      #1a0a0a 30%, 
      #0D1117 60%,
      #0a1a0a 100%
    );
    color: var(--cream);
    min-height: 100vh;
    overflow-x: hidden;
    position: relative;
  }

  body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(ellipse at 20% 20%, rgba(196, 30, 58, 0.1) 0%, transparent 50%),
      radial-gradient(ellipse at 80% 80%, rgba(22, 91, 51, 0.1) 0%, transparent 50%),
      radial-gradient(ellipse at 50% 50%, rgba(255, 215, 0, 0.05) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  /* Copos de nieve */
  .snowflakes {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1000;
    overflow: hidden;
  }

  .snowflake {
    position: absolute;
    top: -5%;
    left: var(--left);
    font-size: var(--size);
    color: rgba(255, 255, 255, 0.8);
    animation: snowfall var(--duration) linear var(--delay) infinite;
    text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
  }

  @keyframes snowfall {
    0% {
      transform: translateY(-10vh) rotate(0deg);
      opacity: 1;
    }
    100% {
      transform: translateY(110vh) rotate(360deg);
      opacity: 0.3;
    }
  }

  /* Guirnalda de luces */
  .top-garland {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 10px 0;
    background: linear-gradient(to bottom, rgba(0,0,0,0.5), transparent);
  }

  .garland-lights {
    display: flex;
    justify-content: center;
    gap: 25px;
    flex-wrap: wrap;
  }

  .light {
    width: 12px;
    height: 18px;
    border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
    animation: twinkle 1s ease-in-out infinite;
    animation-delay: var(--delay);
    box-shadow: 0 0 10px currentColor, 0 0 20px currentColor;
  }

  .light:nth-child(4n+1) { background: #ff0000; color: #ff0000; }
  .light:nth-child(4n+2) { background: #00ff00; color: #00ff00; }
  .light:nth-child(4n+3) { background: #ffff00; color: #ffff00; }
  .light:nth-child(4n) { background: #0066ff; color: #0066ff; }

  @keyframes twinkle {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.9); }
  }

  /* Main content */
  main {
    position: relative;
    z-index: 1;
    max-width: 900px;
    margin: 0 auto;
    padding: 80px 20px 40px;
  }

  /* Hero section */
  .hero {
    text-align: center;
    padding: 60px 20px;
    position: relative;
    animation: fadeInUp 1s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .ornament {
    position: absolute;
    font-size: 3rem;
    top: 20px;
    animation: swing 3s ease-in-out infinite;
  }

  .ornament.left { left: 10%; }
  .ornament.right { right: 10%; animation-delay: 0.5s; }

  @keyframes swing {
    0%, 100% { transform: rotate(-5deg); }
    50% { transform: rotate(5deg); }
  }

  .subtitle-top {
    font-size: 1.2rem;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 10px;
    font-weight: 300;
  }

  .title {
    font-family: 'Playfair Display', serif;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .title-line {
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 700;
    line-height: 1.1;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
  }

  .title-line.accent {
    font-family: 'Great Vibes', cursive;
    font-size: clamp(3rem, 10vw, 6rem);
    color: var(--christmas-red);
    font-weight: 400;
    text-shadow: 
      2px 2px 4px rgba(0,0,0,0.5),
      0 0 30px rgba(196, 30, 58, 0.5);
  }

  /* Efecto PEDA tachada */
  .posada-wrapper {
    position: relative;
    display: inline-block;
  }

  .peda-tachada {
    position: absolute;
    top: -0.5em;
    left: 50%;
    transform: translateX(-50%) rotate(-8deg);
    font-size: 0.45em;
    color: rgba(255, 100, 100, 0.9);
    text-decoration: line-through;
    text-decoration-color: var(--christmas-red);
    text-decoration-thickness: 3px;
    font-family: 'Playfair Display', serif;
    font-weight: 700;
    letter-spacing: 2px;
    white-space: nowrap;
    animation: wiggle 2s ease-in-out infinite;
    text-shadow: 
      1px 1px 2px rgba(0,0,0,0.8),
      0 0 10px rgba(255, 100, 100, 0.5);
  }

  @keyframes wiggle {
    0%, 100% { transform: translateX(-50%) rotate(-8deg); }
    25% { transform: translateX(-50%) rotate(-6deg); }
    75% { transform: translateX(-50%) rotate(-10deg); }
  }

  .year {
    font-size: 1.5rem;
    letter-spacing: 15px;
    color: var(--gold);
    margin-top: 10px;
    font-weight: 600;
  }

  .divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 15px;
    margin: 30px 0;
    color: var(--gold);
    font-size: 1.5rem;
  }

  .divider .line {
    width: 80px;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }

  .invitation-text {
    font-size: 1.3rem;
    line-height: 1.8;
    max-width: 600px;
    margin: 0 auto;
    color: rgba(255, 248, 231, 0.9);
  }

  .invitation-text strong {
    color: var(--gold);
    font-weight: 600;
  }

  /* Info cards */
  .event-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
    margin: 60px 0;
  }

  .info-card {
    background: var(--card-bg);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 215, 0, 0.2);
    border-radius: 20px;
    padding: 30px 20px;
    text-align: center;
    transition: all 0.4s ease;
    opacity: 0;
    transform: translateY(30px);
  }

  .info-card.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .info-card:hover {
    transform: translateY(-10px);
    border-color: var(--gold);
    box-shadow: 0 20px 40px rgba(255, 215, 0, 0.1);
  }

  .info-card .icon {
    font-size: 3rem;
    margin-bottom: 15px;
    display: block;
  }

  .info-card h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 10px;
  }

  .info-card .main-info {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--cream);
  }

  .info-card .sub-info {
    font-size: 1rem;
    color: rgba(255, 248, 231, 0.6);
    margin-top: 5px;
  }

  /* Countdown */
  .countdown-section {
    background: linear-gradient(135deg, 
      rgba(196, 30, 58, 0.2) 0%, 
      rgba(22, 91, 51, 0.2) 100%
    );
    border-radius: 30px;
    padding: 50px 30px;
    text-align: center;
    margin: 60px 0;
    border: 1px solid rgba(255, 215, 0, 0.3);
    opacity: 0;
    transform: scale(0.95);
    transition: all 0.6s ease;
  }

  .countdown-section.visible {
    opacity: 1;
    transform: scale(1);
  }

  .countdown-section h2 {
    font-family: 'Playfair Display', serif;
    font-size: 1.8rem;
    color: var(--gold);
    margin-bottom: 30px;
  }

  .countdown {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .time-block {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .time-block .number {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.5rem, 8vw, 4rem);
    font-weight: 700;
    color: var(--cream);
    background: rgba(0,0,0,0.3);
    padding: 15px 25px;
    border-radius: 15px;
    min-width: 90px;
    text-shadow: 0 0 20px rgba(255, 215, 0, 0.5);
  }

  .time-block .label {
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: rgba(255, 248, 231, 0.7);
    margin-top: 10px;
  }

  .separator {
    font-size: 3rem;
    color: var(--gold);
    animation: blink 1s infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .event-started {
    font-size: 2rem;
    color: var(--gold);
    font-family: 'Playfair Display', serif;
  }

  /* Activities */
  section {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease;
  }

  section.visible {
    opacity: 1;
    transform: translateY(0);
  }

  .activities {
    margin: 60px 0;
    text-align: center;
  }

  .activities h2 {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    color: var(--gold);
    margin-bottom: 40px;
  }

  .activities-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
    gap: 20px;
    max-width: 600px;
    margin: 0 auto;
  }

  .activity {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    padding: 20px;
    background: var(--card-bg);
    border-radius: 15px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
    opacity: 0;
    transform: scale(0.8);
  }

  .activity.visible {
    opacity: 1;
    transform: scale(1);
  }

  .activity:hover {
    transform: scale(1.1);
    border-color: var(--gold);
    background: rgba(255, 215, 0, 0.1);
  }

  .activity-icon {
    font-size: 2.5rem;
  }

  .activity-name {
    font-size: 0.9rem;
    color: rgba(255, 248, 231, 0.8);
  }

  /* Location */
  .location-section {
    margin: 60px 0;
    text-align: center;
  }

  .location-section h2 {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    color: var(--gold);
    margin-bottom: 30px;
  }

  .location-content {
    background: var(--card-bg);
    border-radius: 20px;
    padding: 40px;
    border: 1px solid rgba(255, 215, 0, 0.2);
  }

  .address-box {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    margin-bottom: 30px;
    flex-wrap: wrap;
  }

  .pin-icon {
    font-size: 3rem;
  }

  .address-text h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.5rem;
    color: var(--cream);
  }

  .address-text p {
    color: rgba(255, 248, 231, 0.7);
  }

  .address-detail {
    font-style: italic;
    font-size: 0.9rem;
    margin-top: 5px;
  }

  .map-container {
    margin: 25px 0;
    border-radius: 15px;
    overflow: hidden;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    border: 2px solid rgba(255, 215, 0, 0.3);
    transition: all 0.3s ease;
  }

  .map-container:hover {
    border-color: var(--gold);
    box-shadow: 0 15px 50px rgba(255, 215, 0, 0.2);
  }

  .map-container iframe {
    display: block;
    width: 100%;
    min-height: 300px;
  }

  .map-button {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: linear-gradient(135deg, var(--christmas-green), var(--christmas-green-light));
    color: white;
    padding: 15px 30px;
    border-radius: 50px;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 600;
    transition: all 0.3s ease;
    box-shadow: 0 5px 20px rgba(22, 91, 51, 0.4);
  }

  .map-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(22, 91, 51, 0.6);
  }

  /* Contact */
  .contact-section {
    margin: 60px 0;
    text-align: center;
  }

  .contact-section h2 {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    color: var(--gold);
    margin-bottom: 10px;
  }

  .contact-section > p {
    color: rgba(255, 248, 231, 0.7);
    margin-bottom: 25px;
  }

  .whatsapp-button {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    background: #25D366;
    color: white;
    padding: 18px 40px;
    border-radius: 50px;
    text-decoration: none;
    font-size: 1.2rem;
    font-weight: 600;
    transition: all 0.3s ease;
    box-shadow: 0 5px 25px rgba(37, 211, 102, 0.4);
  }

  .whatsapp-button:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 10px 35px rgba(37, 211, 102, 0.6);
  }

  .whatsapp-icon {
    width: 28px;
    height: 28px;
  }

  /* Dresscode */
  .dresscode {
    margin: 60px 0;
  }

  .dresscode-content {
    background: linear-gradient(135deg, 
      rgba(255, 215, 0, 0.15) 0%, 
      rgba(255, 215, 0, 0.05) 100%
    );
    border: 2px dashed var(--gold);
    border-radius: 20px;
    padding: 30px;
    text-align: center;
  }

  .dresscode-content h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.3rem;
    color: var(--gold);
    margin-bottom: 10px;
  }

  .dresscode-content p {
    color: rgba(255, 248, 231, 0.8);
    font-size: 1.1rem;
  }

  /* Footer */
  footer {
    text-align: center;
    padding: 60px 20px 40px;
    margin-top: 40px;
    border-top: 1px solid rgba(255, 215, 0, 0.2);
  }

  .footer-decoration {
    font-size: 2rem;
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-bottom: 20px;
  }

  .footer-decoration span:nth-child(2) {
    animation: pulse 2s infinite;
    color: var(--gold);
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
  }

  .footer-text {
    font-family: 'Great Vibes', cursive;
    font-size: 2rem;
    color: var(--cream);
    margin-bottom: 10px;
  }

  .footer-small {
    font-size: 0.9rem;
    color: rgba(255, 248, 231, 0.5);
  }

  /* Créditos del desarrollador */
  .dev-credits {
    margin-top: 40px;
    padding-top: 25px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .dev-credits p {
    font-size: 0.85rem;
    color: rgba(255, 248, 231, 0.4);
    margin-bottom: 8px;
  }

  .dev-credits strong {
    color: rgba(255, 248, 231, 0.7);
  }

  .no-pay {
    font-style: italic;
    opacity: 0.7;
  }

  .contact-dev {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    flex-wrap: wrap;
  }

  .facebook-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    background: #1877F2;
    border-radius: 50%;
    transition: all 0.3s ease;
  }

  .facebook-link:hover {
    transform: scale(1.15);
    box-shadow: 0 0 15px rgba(24, 119, 242, 0.6);
  }

  .facebook-icon {
    width: 18px;
    height: 18px;
    color: white;
  }

  /* Responsive */
  @media (max-width: 600px) {
    .ornament { display: none; }
    
    .countdown {
      gap: 5px;
    }

    .time-block .number {
      padding: 10px 15px;
      min-width: 60px;
    }

    .separator {
      font-size: 2rem;
    }

    .garland-lights {
      gap: 15px;
    }

    .light {
      width: 8px;
      height: 12px;
    }
  }
</style>
